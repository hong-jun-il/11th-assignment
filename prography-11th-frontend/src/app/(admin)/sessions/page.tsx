import clsx from "clsx";

export default function Sessions() {
  return (
    <div
      className={clsx(
        "h-full w-full",
        "flex items-center justify-center text-2xl",
      )}
    >
      준비중입니다!
    </div>
  );
}
